import { Profile } from 'selenium-webdriver/firefox';

export class CustomRoll {
    id:number;
    roll_name:string;
    ability:number;
    modifier:number;
    damage_amount:number;
    damage_type:string;
    profile_id:Profile;
}